# Project Notes

- Put publication-ready narrative in `N2O_model_report_zh.md`
- Keep raw input files unchanged in `data/raw/`
- Save cleaned or merged tables to `data/processed/` if you extend the workflow
- The default exported figures and tables are written to `outputs/`
